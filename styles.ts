import { TextStyle, ViewStyle } from 'react-native';

export const textCenter: TextStyle = {
  textAlign: 'center',
};

export const p8: ViewStyle = { padding: 8 };
export const p16: ViewStyle = { padding: 16 };

export const gap16: ViewStyle = { gap: 16 };
export const gap8: ViewStyle = { gap: 8 };
